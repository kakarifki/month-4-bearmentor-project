{
  "name": "budaya-indonesia",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "prestart": "bunx prisma migrate deploy && <seeding disini>",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.0",
    "@prisma/client": "^6.1.0",
    "hono": "^4.6.12",
    "hono-openapi": "^0.3.1",
    "prisma": "^6.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}